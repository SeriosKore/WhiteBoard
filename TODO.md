# 白板项目 · 待办清单（TODO）

> 更新时间：2026-09-12（S1 代码侧完成、拖动问题修复后）
> 当前状态：**S1 代码侧已完整**。`scripts\verify.ps1` **21 项全绿**，单元测试 **225 项**（Core 117 + Rendering 108）。
> 本清单只列**还没做的事**；已完成的内容见 `documents\S1实现进度.md`，验收步骤见 `documents\人工验收清单.md`。

图例：`[ ]` 未开始　`[~]` 部分完成　`[x]` 已完成（保留在此便于对照）

---

## 一、P0 · 必须由你在真机上做（代码侧无法替代）

这些是 S1 结项与 M0 止损闸门的判据，**我做不了，只能你在目标机器上跑**。

- [ ] **1. PoC-A 真机笔迹延迟测量**
  - 做什么：双击 `poc\InkLatency\运行.cmd`，用笔/手指各写 30 秒，切换 baseline / a1 / a2 三条路线，点「导出 CSV」
  - 通过标准：绝对延迟 **≤16.7 ms**；a1/a2 相对 baseline 的差值 **≤2 ms**（这条是关键——证明分层没引入额外延迟）
  - 结果放哪：`验收结果\latency-*.csv`
  - 参考：开发机实测 baseline 9.4/5.6 ms、a1 5.7/7.0 ms、a2 4.0/5.3 ms（仅供参考，不算数）

- [ ] **2. Windows 沙箱零拷贝运行**
  - 做什么：Windows 沙箱（或全新虚拟机）里复制 `publish\selfcontained` 整个文件夹，双击 `验证.cmd`
  - 通过标准：**9 行 PASS**、退出码 0、无缺失 DLL
  - 结果放哪：`验收结果\selftest-*.txt`

- [ ] **3. Windows 10 与 Windows 11 双平台**
  - 做什么：同一份 `publish\selfcontained` 在两个系统各跑一次自检与一次实际书写
  - 通过标准：都能启动、自检通过、能写能擦能存
  - 结果放哪：`验收结果\win10-*.txt`、`win11-*.txt`

- [ ] **4. ProcMon 合规证据**
  - 做什么：Process Monitor 过滤 `Process Name is WhiteBoard.exe`，正常使用 5 分钟
  - 通过标准：`RegSetValue / RegCreateKey / RegDeleteKey` **0 条**；程序文件夹外 `WriteFile` **0 条**（用户显式保存/导出除外）
  - 结果放哪：`验收结果\procmon-*.csv` 或截图

- [ ] **5. 手感反馈（4K 触摸屏 / 手写笔）**
  - 笔宽三档 **3 / 6 / 10**（世界单位）是否合适
  - 橡皮：手指 60 px、笔 24 px、手掌按接触面积放大到 60~420 px
  - 两指手势窗口 **150 ms / 6 点**（想缩放时会不会误起笔、想写字时会不会被吞掉起笔）
  - 笔迹"下笔即固定"是否符合预期（对照 `WhiteBoard.exe --smooth` 的旧手感）
  - 掌拒阈值（触点宽度 45 px）在真实教室里够不够

- [ ] **6. 触屏上的拖动与手势**（我这边只能用鼠标模拟，触摸路径测不到）
  - 用手指拖动对象、两指平移/捏合缩放、拖对象到左侧缩略图、拖动缩略图改页序
  - 结果放哪：截图 + 文字说明

---

## 二、P1 · 代码侧待补的功能缺口

- [ ] **7. 复制 / 粘贴对象（Ctrl+C / Ctrl+V）**
  - 现状：只有「克隆」（原地偏移复制），没有剪贴板语义
  - 为什么需要：跨页搬运目前的路径是"拖动到缩略图"或「移到…」；而"复制一份到另一页"只能靠克隆 + 搬运两次
  - 做法：进程内剪贴板（文档级，不碰系统剪贴板——系统剪贴板在部分环境会写注册表/触发其他程序）
  - 预估：0.5 天　依赖：无
  - 验收：复制 → 切页 → 粘贴，对象出现在新页且 Id 不冲突（用 `.wb` 存读往返验证）

- [ ] **8. 方向键微调选中对象的位置**
  - 现状：只能拖动，拖不准（1 px 级微调做不到）
  - 做法：方向键移动 1 世界单位、`Shift+方向键` 移动 10；每次按键合并成一条撤销项（或按键序列结束再入栈，避免撤销栈被刷屏）
  - 预估：0.5 天　依赖：无
  - 验收：选中对象 → 按右方向键 → X 增大 1；撤销一次回到原位（不是回到中间某步）

- [ ] **9. 页面缩略图上的删除按钮**（计划书 §5.12 的原方案）
  - 现状：删除入口在工具栏「删除页」+ 确认框
  - 计划书要求：每个缩略图带删除按钮，点击后确认，且提供会话内「最近删除」可一键恢复
  - 说明：我用了更简单的"单步撤销页面操作"，**功能上够用但不完全等同**（详见"三、待你裁决"）
  - 预估：0.5 天（只做删除按钮）/ 1.5 天（含最近删除）

- [ ] **10. 橡皮的延迟烘焙**（ADR-18）
  - 现状：擦除只往对象追加遮罩；遮罩数超过 32 时会提示"可烘焙"，但**没有真正烘焙**
  - 为什么要做：一条笔迹被反复擦几百次后，几何差集的重算代价会累积（实测最坏 1.07 ms/次，仍可用，但不理想）
  - 做法：遮罩数超阈值时把"原几何 − 遮罩"烘焙成新的静态几何，遮罩清空，几何指纹换新
  - 预估：1 天　依赖：无
  - 验收：连续擦同一条笔迹 100 次后，单帧几何重算耗时仍在 2 ms 内；烘焙前后**画面完全一致**（逐像素比较）

- [ ] **11. 页面背景类型**（`Page.BackgroundType` 目前恒为 `"solid"`）
  - 现状：字段已有、序列化已有，但没有方格纸/横线纸等背景
  - 预估：1 天（含 `.wb` 兼容与缩略图）

- [ ] **12. 「最近文件」的缩略图预览**
  - 现状：最近文件列表只有文字（名称 + 页数 + 对象数 + 时间）；`.wb` 包里**已经存了缩略图**（`preview.png`），但没有用起来
  - 做法：`WbPackage.TryReadPreview` 已有实现，接到列表项上即可
  - 预估：0.5 天

---

## 三、P1.5 · 我偏离了计划书、需要你裁决的 5 处

详见 `documents\人工验收清单.md` §"P1.5"。改回去的工作量都不大（每项 ≤0.5 天）。

- [ ] **13. 页面级操作的可撤销方式**：计划书 = 不进撤销栈 + 会话内「最近删除」；我做成了 = 全部可撤销 + 状态栏「撤销页面操作」按钮
- [ ] **14. 删除最后一页**：计划书 = 自动补一个空白页；我做成 = 拒绝删除（按钮变灰）
- [ ] **15. 删除当前页后停在哪一页**：计划书 = 相邻页（优先前一页）；我做成 = 同一格（原来的下一页）
- [ ] **16. 页面操作是否占用 Ctrl+Z**：我明确**不占用**（Ctrl+Z 只撤销当前页的内容改动）
- [ ] **17. 追加导入的入口**：计划书 = 导入完成后弹「已追加 n 页 · 撤销」提示条；我做成 = 文件栏按钮 + 状态栏撤销

---

## 四、P2 · 计划内但排在后面几期的

### S2（复杂工具，12 周）
- [ ] 旋转 / 手柄缩放（S1 只能整体移动，不能转、不能拉伸）
- [ ] 图片对象（含 `images/` 资源机制；`.wb` 格式已为它预留了扩展位）
- [ ] 数学公式、化学式、几何辅助线
- [ ] 右键 / 长按 500ms 菜单（复制/粘贴/删除/克隆/置顶/置底/编辑文本）
- [ ] 4K 精细适配（S1 只保证不错位；字号、按钮密度需要按 4K 调）
- [ ] 跨显示器 DPI 变化（S1 只保证单显示器正确，这是**已知限制**）
- [ ] 对象按资源 ID + 缩放分档的图像缓存

### S3（插件生态，12 周）
- [ ] 脚本沙箱（Jint/JS）通道、插件清单与权限模型
- [ ] 声明式图形（第三方新增图形类型）
- [ ] 插件预检与验证流水线

### S4（生态与交付，8 周）
- [ ] 签名与白名单、批量部署工具
- [ ] i18n（多语言）
- [ ] 稳定性收尾与交付

---

## 五、P3 · 技术债与已知问题

- [ ] **18. 本机 .NET SDK 10.0.400 安装不完整**
  - 现象：缺 workload locator 目录 → **解决方案级**构建（`.sln`/`.slnx`）静默失败
  - 现状：`scripts\verify.ps1` 改为逐工程构建，**不阻塞开发**
  - 修复：需管理员重装 SDK；修好后可用解决方案构建
- [ ] **19. `--render-smoke` 的项数会随参数变化**（指定 `--out` 时 14 项，不指定时 13 项）
  - 现状：文档已注明；若要统一，把"写出 PNG"改成用临时文件
- [ ] **20. 保存精度取整导致存读有 16/1024000 像素差**
  - 现象：坐标按 0.0001 世界单位取整 → 存读后渲染有 0.002% 的像素不同（肉眼不可见）
  - 取舍：取整是为了减小文件体积；若要**完全**逐位一致，改为保留全精度浮点（文件会大一些）
  - 决策点：是否需要"位级一致"（例如未来要做文件校验）？
- [ ] **21. `ui-tools-test.ps1` / `ui-drag-test.ps1` 需要交互式桌面**
  - 现状：无桌面会话时如实报告"跳过"，不算通过也不算失败
  - 影响：CI 若不提供桌面会话，这两步永远跳过（但离屏的其它 19 项仍然有效）
- [ ] **22. 无「撤销/重做」的菜单项与按钮状态提示**
  - 现状：按钮的启用/禁用已正确；但没有"撤销：擦除"这类**动作名**提示（`PageCommandStack.NextUndoName` 已经实现，只差显示）
  - 预估：0.2 天

---

## 六、已完成（对照用，不必再做）

- [x] 文件层：`.wb` 包（ZIP+JSON、原子写、版本号、损坏可解释）、保存/另存为/打开、自动保存与崩溃恢复、PNG 导出、追加导入、打开数据目录
- [x] 选择工具：点选（取最上层）/ 框选 / 圈选 / 多选 / 拖动移动 / 删除 / 置顶置底 / 克隆 / 改笔宽 / Ctrl+A / Esc
- [x] 页面外壳：目录 + 内容裁剪缩略图、翻页、新建/复制/删除页、跨页搬运（拖动 + 「移到…」）、**拖动缩略图改页序**
- [x] 文本工具：就地 `TextBox`（中文 IME 可用）、字号三档、粗体、多行、点已有文字即改那一段、不参与橡皮擦除
- [x] 收尾：全屏、最近文件、批量导出、快捷键说明、用户手册、PoC-B 退化用例（13 项）
- [x] 输入链路：笔/触摸/鼠标统一路由、合成鼠标抑制、双指手势（手势窗口 150ms）、掌拒、手掌擦除
- [x] 笔迹稳定性：**下笔即固定**（ADR-20）——已画部分零改动，可用 `--smooth` 对比旧行为
- [x] 自检体系：`verify.ps1` 21 项 / 单元测试 225 项 / 渲染 14 项 / 文件+页面+文本 21 项 / 画布交互 9 项 / 拖动交互 6 项 / 环境 9 项

---

## 七、怎么用这份清单

1. **每次要接着做时**：从第一节自上而下推进；做完一项把 `[ ]` 改成 `[x]`，并在 `documents\S1实现进度.md` 里记一句（含实测数字）。
2. **每完成一项**：跑 `scripts\verify.ps1` 保持 21 项全绿；新增行为要补**可复核的证据**（单元测试 / 自检项 / 截图），不要只写"已实现"。
3. **动过 UI 之后**：一定要跑 `scripts\ui-tools-test.ps1` 与 `scripts\ui-drag-test.ps1`——
   这两个脚本是为"离屏自检全绿但真实输入是坏的"这类问题专门建的（已经抓到过 3 次）。
4. **备份**：`scripts\backup.ps1` 会生成带时间戳的副本 + zip（不含可重建的 `bin/obj/publish`）。
