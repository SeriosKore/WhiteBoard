# 白板项目 · 文档索引

> 本文件夹是**开发文档**。所有开发约束与计划以 `白板开发总路线.md` 为唯一基线。

## 有效开发文档

| 文档 | 内容 | 状态 |
|---|---|---|
| **`白板开发总路线.md`** | **唯一技术基线**：四条硬约束、技术选型、关键不变量、部署模型、插件体系、架构分层、验证体系、分期规划、风险与 ADR | ✅ 生效 |
| `M0可行性分析与待决策清单.md` | M0 止损闸门：四项 PoC 的可行性分析、验收口径、待决策清单（P0 已决策，P1/P2 待定） | ✅ 生效 |
| `M0进展报告.md` | **M0 实施进展**：已完成产出、PoC-D 硬结论、4 项需回写设计的实测事实、一键验证流水线 | ✅ 生效 |
| `S1产品与技术评审.md` | 一期的产品评审 + 技术评审：产品缺口（G1~G6）、技术模糊点（T1~T17）、决策结果、专项评估 | ✅ 生效 |
| **`发布说明_preview-v0.1b.md`** | **本版发布说明**：内容清单、验证证据、安装方式、已知限制、真机验收待办（GitHub Release 正文即此文件） | ✅ 生效 |
| **`../TODO.md`** | **待办清单**：你必须做的真机事项、代码侧功能缺口、偏离计划书待裁决的 5 处、S2/S3/S4 计划内、技术债 | ✅ 生效 |
| **`用户手册.md`** | **给老师与部署者的使用手册**：界面导览、逐步操作、多页面、文件与导出、自动保存、主题自定义、常见问题、S1 已知限制、部署者清单与命令行入口 | ✅ 生效 |
| **`人工验收清单.md`** | **你要做什么 + 怎么验收**：5 分钟双击核验、必须由人做的 P0/P1 事项、决策复核、已产出的证据 | ✅ 生效 |
| `需求对照与差异清单.md` | **原始需求 vs 当前规划**逐条对照：一致 / 延后 / 语义不一致（**12 项待决策 D1~D12**）/ 已决策变更 / 新增项 | ✅ 生效 |
| `计划书_S1_基础版.md` | 第 1 期：能上课的稳定白板（10 周）· **v1.5**（38 任务 / 26 验收） | ✅ 生效 |
| `S1实现进度.md` | **S1 实现进度盘点**：逐任务状态（24 ✅ / 7 🟡 / 7 ⬜）、本轮新增模块、测试抓到的缺陷 | ✅ 生效 |
| `计划书_S2_复杂工具.md` | 第 2 期：全形状、图片、数学、化学、几何辅助、4K 适配（12 周） | ✅ 生效 |
| `计划书_S3_插件生态.md` | 第 3 期：第三方插件契约、脚本沙箱、预检与验证流水线（12 周） | ✅ 生效 |
| `计划书_S4_生态与交付.md` | 第 4 期：签名与白名单、批量部署、i18n、稳定性收尾、交付（8 周） | ✅ 生效 |

## 评审记录（非开发基线）

| 位置 | 内容 |
|---|---|
| `评审记录/` | 语言与技术选型阶段的评审记录。其中关于**电脑环境与磁盘**的描述（还原卡、软件盘与盘符、目标机缺 VC++ 组件等）**仅用于当时的语言选择论证**，**已不作为开发约束** |

## 四条硬约束（速查）

| # | 约束 | 要点 |
|---|---|---|
| **C1** | 免环境部署 | 不装运行时/安装包、免管理员、离线可用；不依赖目标机未预装的组件（含 VC++ 可再发行组件） |
| **C2** | 不写注册表 | 应用与插件均不得写注册表，也不得依赖注册表存任何状态 |
| **C3** | 数据自包含 | 程序产生的所有数据文件都在 **exe 同级目录或其子目录**内；**用户显式保存的画板文件除外** |
| **C4** | 插件开放 | 第三方走沙箱通道，官方走进程内通道 |

## 分期总览

| 期 | 主题 | 工期（2 人） |
|---|---|---:|
| S1 | 基础版 | 10 周 |
| S2 | 复杂工具 | 12 周 |
| S3 | 插件生态 | 12 周 |
| S4 | 生态与交付 | 8 周 |
| | **合计** | **42 周 ≈ 10.5 个月（~19~20 人月）** |

## 工作区其他文件

| 文件 | 说明 |
|---|---|
| `../color.txt` | **主题色规范文件**（JSON）：程序默认值的来源，构建时嵌入为内置默认；运行时由 `<exe同级>\data\theme\color.txt` 覆盖。自 S2 起用于保存用户预设 |
| `../旧文件/` | 已废弃的旧白板项目归档（仅源码，零复用） |
| `../验收结果/` | **人工验收交接目录**：已放入真实窗口截图与离屏渲染成图，你把自己的测量结果也丢这里 |
| `../artifacts/` | 自动验证的产物（`verify-report.txt`、`render-smoke.png`、`pe-scan-*.txt`、`selftest-*.txt`） |

### 常用脚本与命令（一键入口）
| 命令 | 作用 |
|---|---|
| `powershell -ExecutionPolicy Bypass -File scripts\verify.ps1` | **总验证**：构建 7 工程 + 2 套测试 + PoC 冒烟 + 自包含发布 + PE 审计 + 自检 + 渲染链路自检 + **文件层自检** + 数据目录合规 → `artifacts\verify-report.txt`（当前 **21 项全绿**） |
| `WhiteBoard.exe --self-test --report <txt>` | 免环境自检（9 项）：数据目录/编码往返/ANSI 回退/路径穿越/主题/无第三方原生库 |
| `WhiteBoard.exe --render-smoke --out <png> --report <txt>` | **渲染链路自检**（13 项）：用生产链路合成一次真实绘制并逐区域数像素 |
| `WhiteBoard.exe --file-smoke [--report <txt>] [--keep <目录>]` | **文件层 + 页面 + 文本自检**（21 项）：保存 → 重新打开 → **逐像素比较**，含导出、自动保存、页面操作、跨页搬运、逐页缩略图、追加导入 |
| `WhiteBoard.exe --demo` | 载入示例画板（波形笔迹 + 形状 + 被橡皮擦断的一条 + 第 2 页） |
| `WhiteBoard.exe --demo-select` | 同上，并预先选中两个对象（验收选中框绘制） |
| `WhiteBoard.exe --size 1000x700` | 以指定尺寸启动（验收"窄屏布局是否错位"时用） |
| `powershell -ExecutionPolicy Bypass -File scripts\ui-drag-test.ps1 -ExePath <exe> [-OutDir <目录>]` | **拖动交互自检**：真鼠标拖对象 / 中键平移 / 拖到缩略图跨页搬运 / 拖缩略图改页序（6 项） |
| `powershell -ExecutionPolicy Bypass -File scripts\ui-tools-test.ps1 -ExePath <exe> [-OutDir <目录>]` | **画布交互自检**：真实鼠标/键盘逐个确认画笔/矩形/椭圆/橡皮/文本可用（9 项）；无桌面会话时如实报告“跳过” |
| `powershell -ExecutionPolicy Bypass -File scripts\screenshot.ps1 -ExePath <exe> -OutPath <png> [-Demo] [-Size 1000x700] [-AppArgs "--demo-select"]` | 抓真实窗口截图，并报告"非黑像素比例"以判定截图是否有效 |
| `powershell -ExecutionPolicy Bypass -File scripts\publish-release.ps1 -Tag <tag> -Name <标题> -NotesFile <md> [-AssetPath <zip>]` | **发布 GitHub Release**：创建/复用 Release 并上传附件（复用 git 凭据，不回显 token，可重复执行） |
| `powershell -ExecutionPolicy Bypass -File scripts\backup.ps1 [-IncludePublish]` | **项目备份**：带时间戳的完整副本 + zip（默认不含可重建的 bin/obj/publish；加 `-IncludePublish` 连可运行产物一起，约 62 MB 压缩包） |

> 无界面入口（self-test / render-smoke / file-smoke）**不会构造主窗口**，因此可以在没有桌面会话的机器上跑。

## 下一步

S1 的代码侧功能已全部完成（含 PoC-B 退化用例）；
之后推进 `人工验收清单.md` §3 的真机事项
（PoC-A 延迟测量、Windows 沙箱、Win10/Win11 双平台、ProcMon 合规证据、手感反馈）。


